---
title: Setup
description: How to setup the project locally.
---

# Setup Guide

This project is built using a modern Turborepo monorepo.

### Prerequisites
- Node.js 18+
- pnpm
- Python 3.10+ (for API)

### Installation
Run `pnpm install` in the root of the project to install all dependencies across the workspaces.
